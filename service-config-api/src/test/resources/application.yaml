server:
  port: 0
logging:
  level:
    com.mf.auth: DEBUG
management:
  endpoint:
    health:
      probes:
        enabled: true
swagger:
  url: /swagger-ui/index.html
  title: Service Config API [TEST PROFILE]
  description: Service Config API
  version: "1.0.0-SNAPSHOT"
  controllersPackage: com.mf.serviceconfig.rest
